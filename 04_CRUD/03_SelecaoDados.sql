# Selecionando tudo 
USE cadastro;

# Seleciona todas as colunas da tabela pessoas
SELECT * FROM pessoas; 

# Seleciona apenas as colunas nome e cpf da tabela pessoas
SELECT nome, cpf FROM pessoas;

# Seleciona apenas o rg
SELECT nome, rg FROM pessoas;