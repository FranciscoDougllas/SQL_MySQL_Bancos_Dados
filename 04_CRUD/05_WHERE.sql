-- O comando WHERE é utilizado em SQL para filtrar os registros de uma tabela, retornando apenas aqueles que atendem a uma condição específica.
-- Por exemplo, para selecionar apenas as pessoas com limite maior que 3000:
-- SELECT * FROM pessoas WHERE limite > 3000;
-- Assim, o WHERE ajuda a buscar dados mais específicos dentro das tabelas.
USE 